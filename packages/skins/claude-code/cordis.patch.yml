# claude-code skin bundle patch: inserts its dsh.client row into the web plugin roster.
# Install this leaf package from a complete repository clone with a local
# link path. Mutual exclusion between skins is managed by scripts/dsh-skin.
- insert:
    - id: ui-skin-claude-code
      name: '@deepseek-ai/dsh-client-ui-skin-claude-code'
